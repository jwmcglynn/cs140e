1) Bring the enum values into scope with "use Duration::*" so that Seconds, MilliSeconds, etc. compile in main.
2) Implement the PartialEq trait so that assert_eq can compare the values.
3) Derive the debug trait so that assert_eq can print failures.
